import React from 'react'
import { AnswerResult } from '../../../../types'

const FeedbackView = ({ result, onNext }: { result: AnswerResult | null; onNext: () => Promise<void> }) => {
  return <div>FeedbackView</div>
}

export default FeedbackView
